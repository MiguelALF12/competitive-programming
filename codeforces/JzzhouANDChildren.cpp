#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    queue<int> children;
    int n,m,aux;

    cin>>n>>m;

    while(n){
        cin>>aux;
        push.children(aux);
        n--;
    }
    int ind=0;
    while(true){
        if()
        children.front() += m;

    }
    
}